# frozen_string_literal: true

source "https://rubygems.org"

gem "faraday", "~> 2.0"

group :development, :test do
  # ローカルで .env を読むためだけに使う（Actions では不要）
  gem "dotenv"
  # テスト用
  gem "rspec", "~> 3.13"
end
